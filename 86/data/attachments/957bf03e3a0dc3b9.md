# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: bloque-b/anulacion.spec.ts >> Reservas — anulacion >> Hotel: una reserva emitida se puede cancelar y el portal lo refleja
- Location: tests/bloque-b/anulacion.spec.ts:350:7

# Error details

```
Error: El listado tiene que ofrecer el hotel 5003

expect(locator).toBeVisible() failed

Locator: locator('a[href*=\'hoteldetail.aspx\'][href*=\'hotel=5003\']').first()
Expected: visible
Timeout: 60000ms
Error: element(s) not found

Call log:
  - El listado tiene que ofrecer el hotel 5003 with timeout 60000ms
  - waiting for locator('a[href*=\'hoteldetail.aspx\'][href*=\'hotel=5003\']').first()

```

```yaml
- link " hello@amv.travel":
  - /url: mailto:hello@amv.travel
- link "Emergencia 24hs  +54 9 11 3256 2827":
  - /url: https://api.whatsapp.com/send/?phone=5491132562827&text=Hola%20AMV%20Travel&type=phone_number&app_absent=0
  - text: Emergencia 24hs 
  - strong: +54 9 11 3256 2827
- text: Entorno de test
- combobox:
  - option "Buscar agencia" [selected]
- combobox "Buscar agencia..."
- text: ⨯
- combobox:
  - option "Usuario" [selected]
- combobox:  Usuario
- complementary:
  - link:
    - /url: /online/Default.aspx
  - list:
    - listitem:
      - link "Inicio":
        - /url: /online/Default.aspx
    - listitem:
      - link "Multidestino":
        - /url: /online/tourall.aspx?country=10&city=5000&tour=0&resident=false
    - listitem:
      - link "Tarifario":
        - /url: /online/defaulttariff.aspx?country=10&city=5000&from=25-09-2026&to=25-03-2028&resident=false&tab=tour&tourId=0
    - listitem:
      - link "Series":
        - /url: /online/serieAll.aspx
    - listitem:
      - link "Reservas":
        - /url: /online/bookinghistory.aspx
    - listitem:
      - link "Cotizaciones":
        - /url: /online/quotehistory.aspx
  - list:
    - listitem:
      - link:
        - /url: https://qa.amv.travel/online/ShoppingCartPage.aspx
  - list:
    - listitem:
      - link "M 0.50 ":
        - /url: "#"
  - list:
    - listitem:
      - link "Pablo ":
        - /url: "#"
  - list:
    - listitem:
      - link "ES ":
        - /url: "#"
- complementary:
  - text: Destinos
  - textbox: AUTO-QA NO TOCAR - Park Hyatt Palacio Duhau, Buenos Aires, Argentina
  - text: Check-In | Check-Out
  - paragraph:
    - textbox: 25/10/2026 - 26/10/2026
    - text: 
  - text: Viajeros
  - textbox [disabled]: 2 adultos · 1 habitación
  - list:
    - listitem:
      - link:
        - /url: "#"
  - button " Buscar"
- article:
  - 'heading "AUTO-QA NO TOCAR - Park Hyatt Palacio Duhau: 0 Resultados" [level=1]'
  - text: Ordenar por
  - link "A-Z":
    - /url: javascript:__doPostBack('ctl00$cphMain$lnkSortByAz','')
  - link "Precio":
    - /url: javascript:__doPostBack('ctl00$cphMain$lnkSortByPrice','')
  - paragraph: No se encontraron resultados
- text: AMV. TRAVEL  Avenida Córdoba 673 1°B
- link " 54 11 50313060":
  - /url: tel:54 11 50313060
- link "hello@amv.travel":
  - /url: mailto:hello@amv.travel
```

# Test source

```ts
  81  |     for (let i = 0; i < adultos; i++) await sumar('adults').click();
  82  | 
  83  |     await expect(this.page.locator('#rooms')).toHaveValue(String(habitaciones));
  84  |     await expect(this.page.locator('#adults')).toHaveValue(String(adultos));
  85  |   }
  86  | 
  87  |   /**
  88  |    * Elige check-in y check-out en el calendario de rango.
  89  |    *
  90  |    * Como en el buscador de servicios, no se escribe la fecha: el widget la
  91  |    * reescribe con la suya al perder el foco. Se abre y se clickean los dos dias.
  92  |    */
  93  |   /**
  94  |    * Elige el rango de fechas en el calendario del buscador.
  95  |    *
  96  |    * **El widget muestra dos meses a la vez** —`.drp-calendar.left` y `.right`— y
  97  |    * cada uno trae sus desplegables de mes y anio. Antes se buscaba el dia con
  98  |    * `td.available` sobre el picker entero, y eso funciona de casualidad: mientras
  99  |    * la fecha caiga en el mes de la izquierda no se nota, pero apenas cae en el
  100 |    * otro, `.first()` se queda con el mismo numero de dia **del mes equivocado**.
  101 |    *
  102 |    * Salio a la luz con la anulacion, que reserva a 30 dias: buscando el 7 de
  103 |    * octubre elegia el 7 de septiembre, que tambien estaba disponible. Los flujos a
  104 |    * 7 dias nunca lo mostraron. Es un defecto del test, no del sistema.
  105 |    *
  106 |    * Ahora se ubica el calendario que muestra el mes buscado y, si ninguno de los
  107 |    * dos lo muestra, se lo lleva ahi con los desplegables — que es lo que hace una
  108 |    * persona.
  109 |    */
  110 |   async elegirFechas(entrada: Date, salida: Date) {
  111 |     await this.page.locator(this.campoFechas).click();
  112 |     const picker = this.page.locator('.daterangepicker:visible').first();
  113 |     await expect(picker, 'El campo de fechas tiene que abrir el calendario').toBeVisible();
  114 | 
  115 |     await this.elegirDia(picker, entrada);
  116 |     await this.elegirDia(picker, salida);
  117 |   }
  118 | 
  119 |   /** Elige un dia dentro del calendario que muestra su mes. */
  120 |   private async elegirDia(picker: Locator, fecha: Date) {
  121 |     const lados = ['left', 'right'];
  122 | 
  123 |     const calendarioDelMes = async (): Promise<Locator | null> => {
  124 |       for (const lado of lados) {
  125 |         const cal = picker.locator(`.drp-calendar.${lado}`).first();
  126 |         if (!(await cal.count())) continue;
  127 |         const mes = await cal.locator('select.monthselect').inputValue().catch(() => '');
  128 |         const anio = await cal.locator('select.yearselect').inputValue().catch(() => '');
  129 |         if (Number(mes) === fecha.getMonth() && Number(anio) === fecha.getFullYear()) return cal;
  130 |       }
  131 |       return null;
  132 |     };
  133 | 
  134 |     let cal = await calendarioDelMes();
  135 |     if (!cal) {
  136 |       // Ninguno de los dos meses visibles sirve: se lo lleva con los desplegables.
  137 |       const izquierda = picker.locator('.drp-calendar.left').first();
  138 |       await izquierda.locator('select.yearselect').selectOption(String(fecha.getFullYear()));
  139 |       await izquierda.locator('select.monthselect').selectOption(String(fecha.getMonth()));
  140 |       cal = await calendarioDelMes();
  141 |     }
  142 | 
  143 |     expect(
  144 |       cal,
  145 |       `El calendario tiene que poder mostrar ${fecha.getDate()}/${fecha.getMonth() + 1}/` +
  146 |       `${fecha.getFullYear()}`,
  147 |     ).not.toBeNull();
  148 | 
  149 |     await cal!.locator('td.available:not(.off)')
  150 |       .filter({ hasText: new RegExp(`^${fecha.getDate()}$`) }).first().click();
  151 |   }
  152 | 
  153 |   /**
  154 |    * El hotel de prueba esta publicado solo para no residentes
  155 |    * (`Hotel.ForResidents = 0`), asi que la busqueda tiene que salir como tal.
  156 |    * El combo no es visible para el usuario: se exige el valor por defecto para
  157 |    * que un cambio en esa configuracion no rompa el test en silencio.
  158 |    */
  159 |   async verificarResidente(valorEsperado = 'US') {
  160 |     await expect(
  161 |       this.page.locator(this.comboResidente).first(),
  162 |       'La busqueda tiene que salir como No Residente: el hotel de prueba no esta publicado para residentes',
  163 |     ).toHaveValue(valorEsperado);
  164 |   }
  165 | 
  166 |   /** Presiona Buscar y espera la pantalla de resultados. */
  167 |   async buscar() {
  168 |     await this.page.locator(this.btnBuscar).first().click();
  169 |     await this.page.waitForURL(/hotelwithintegration/i, { timeout: 120_000 });
  170 |     await esperarFinDeCarga(this.page);
  171 |   }
  172 | 
  173 |   /** La card del hotel en el listado de resultados. */
  174 |   card(nombre: string): Locator {
  175 |     return this.page.locator('div').filter({ hasText: nombre }).last();
  176 |   }
  177 | 
  178 |   /** Entra a la ficha del hotel desde el listado, por el boton "Ver mas". */
  179 |   async abrirFicha(id: number) {
  180 |     const enlace = this.page.locator(`a[href*='hoteldetail.aspx'][href*='hotel=${id}']`).first();
> 181 |     await expect(enlace, `El listado tiene que ofrecer el hotel ${id}`).toBeVisible({ timeout: 60_000 });
      |                                                                         ^ Error: El listado tiene que ofrecer el hotel 5003
  182 |     await enlace.click();
  183 |     await this.page.waitForURL(/hoteldetail/i, { timeout: 90_000 });
  184 |     await esperarFinDeCarga(this.page);
  185 | 
  186 |     // Las tarifas de las habitaciones llegan por AJAX despues de cargar la
  187 |     // pagina: sin esta espera la ficha se lee sin ninguna habitacion.
  188 |     await this.page.waitForFunction(() => {
  189 |       const w = window as any;
  190 |       return typeof w.jQuery === 'undefined' || w.jQuery.active === 0;
  191 |     }, undefined, { timeout: 60_000 });
  192 |     await expect(
  193 |       this.page.locator("[onclick*='Guardar']").first(),
  194 |       'La ficha tiene que ofrecer habitaciones para las fechas elegidas',
  195 |     ).toBeVisible({ timeout: 60_000 });
  196 |   }
  197 | 
  198 |   /**
  199 |    * Prefijo de id de la fila de tarifa de una habitacion.
  200 |    *
  201 |    * La ficha codifica en el id todo el contexto de la tarifa:
  202 |    * `hotel-habitacion-tipoTarifa-adultos-menores-origen-`. Se lo lee del DOM en
  203 |    * vez de armarlo, porque el tipo de tarifa lo decide el sitio a partir de la
  204 |    * cantidad de adultos (2 adultos -> Doble) y no queremos fijarlo en el test.
  205 |    *
  206 |    * La clase de ese input es el numero de habitacion, y una clase que empieza
  207 |    * con digito no se puede escribir como `.9193`: va por `[class~="9193"]`.
  208 |    */
  209 |   private async prefijoDeTarifa(habitacion: number): Promise<string> {
  210 |     const campo = this.page.locator(`input[class~="${habitacion}"]`).first();
  211 |     const id = await campo.getAttribute('id');
  212 |     expect(id, `La ficha tiene que traer la habitacion ${habitacion}`).toBeTruthy();
  213 |     return id!;
  214 |   }
  215 | 
  216 |   /** Precio por habitacion que muestra la ficha, tal como lo ve la persona. */
  217 |   async precioDeLaHabitacion(habitacion: number): Promise<string> {
  218 |     const prefijo = await this.prefijoDeTarifa(habitacion);
  219 |     return this.page.locator(`[id="${prefijo}-TotalxPax"]`).first().inputValue();
  220 |   }
  221 | 
  222 |   /** El tipo de tarifa que el sitio eligio segun la cantidad de adultos. */
  223 |   async tipoDeTarifa(habitacion: number): Promise<string> {
  224 |     const prefijo = await this.prefijoDeTarifa(habitacion);
  225 |     return prefijo.split('-')[2] ?? '';
  226 |   }
  227 | 
  228 |   /**
  229 |    * Carga la cantidad de habitaciones y confirma.
  230 |    *
  231 |    * La cantidad no se escribe: el input esta `disabled` y se sube con el boton
  232 |    * `+`, que tiene id `{prefijo}-suma` y llama a SumarRestar. Confirmar dispara
  233 |    * `Guardar({habitacion})`, que lee la cantidad del input cuya clase es el
  234 |    * numero de habitacion.
  235 |    */
  236 |   async reservarHabitacion(habitacion: number, cantidad: number) {
  237 |     const prefijo = await this.prefijoDeTarifa(habitacion);
  238 |     const mas = this.page.locator(`[id="${prefijo}-suma"]`).first();
  239 |     for (let i = 0; i < cantidad; i++) await mas.click();
  240 | 
  241 |     await expect(
  242 |       this.page.locator(`[id="${prefijo}"]`).first(),
  243 |       `La ficha tiene que quedar con ${cantidad} habitacion(es) elegidas`,
  244 |     ).toHaveValue(String(cantidad));
  245 | 
  246 |     await this.page.locator(`input[type='button'][onclick*='Guardar(${habitacion},']`).first().click();
  247 |     await esperarFinDeCarga(this.page);
  248 |   }
  249 | }
  250 | 
```